import React from 'react'
import {connect} from 'react-redux'

const Basket = () => {
    return (
        <div>
            Корзина
        </div>
    )
}

export default connect()(Basket)